// riscv64-unknown-elf-gcc -march=rv32i   -mabi=ilp32   -O2 -c test1.c
// riscv64-unknown-elf-objdump -d -M no-aliases test1.o      (objdump reassembly)

// check binary file content: 
# 只看纯二进制，去掉左边的地址和右边的ASCII
xxd -b -c 1 test1.o | cut -c 11-63

xxd -b -l test1.o



dive deep into how c program compiled, use what kind of instr or instr_combo
I already know that compiler should do:
Compilation (3 checking (if error, report error), intermediate language -> optimization) -> assembly(.s) -> .o(binary)
Linking

test.c (源代码)
   ↓ 预处理 (preprocessing)
test.i (预处理后的代码)
   ↓ 编译 (compilation)
test.s (汇编语言文件，文本格式)  ← **这才是汇编文件**
   ↓ 汇编 (assembly)
test.o (目标文件，二进制机器码)  ← **不是汇编文件**
   ↓ 链接 (linking)
test (可执行文件)

I could use objdump push .o file (not linked) back to assembly,
 check which specific instr compiler use for explain this c program
------------------------------------
Notice:

Variable:
RISC-V has 2 sets of integer ABI:
ILP32 for RV32   LP64 for RV64
ILP: Integer Long Pointer

except char(1 Byte), diff ABI diff variable size

range condition judgement usually use transfer range + one end compare (another end use big nagative num)


------------------------------------
Instr level arrangement (Compiler behavior .c -> .s(RV)):

=> 1.Constant:
-> How to load 32 bits constant: if the number is longer than imm bits, we could use (e.g. lui + addi), load long imm to register
-> How to load 64 bits constant (in RV64? in RV32?)

=> 2.Variable:
ABI define size

=> 3.Variable distribution:
priorly load to ram, if needed, load to register

static data; heap; stack; text
 3 ram area for data storage: static data, heap(grow higher), stack(grow to lower addr)
 1 for text
global variable, static domestic variable -> static data area
non-static domestic variable -> stack
dynamic distributed variable -> heap

=> 4.Variable access:

=> 5.Operation & Cal:
alus logics   bit logics  shift  compare  num-expansion
RV32 cal 64bit add: (a1 a0) <- (a1, a0) + (a3, a2);
a5 = a2 + a0, compare a5, a0, if a5<a0, carry occurs
mv a0 a5;
a1 = a1 + a3 + carry;

as for signed add & unsigned add, for hardware they look same

=> 6.Condition (if, case..)
switch case: branch table(case table) + return table (the case shows in course shows compiler doesn't use tremendous beqs)
            if values in case are massively discreted, compiler step back to use traditional beq method 

=> 7.Circular (while for)

=> *** Undefined behaviour *** (from c->s point of view)  somtimes for compatibility of multiple ABIs
undefined behaviour is bad behaviour for machine and program, diff ABI(ISA), diff compiler could judge how to deal with it arbitarily.

Hardware doesn't care signed or unsigned add, so add overflow is undifined behaviour
RISC-V shift define maximum shift amount is 5bit-num(32 bits), shift amount beyond 32 bits is undifined behaviour
Divided by 0: C undifined behaviour, RISC-V defines constant -1 as result

=> 8. function calling
system lib is dynamic lib
a0-a7 use to transform parameter, a0/a1 use to transfer return values
the way to transfer para depend on the LENGTH and amount of para
in the case shows in the course, when I call a function which includes 10 register, the first 8 para are loaded into a0-a8 and 
the remaining 2 parameters are loaded into memory(stack area)
and the compiler distribute spare space in stack in advance for returned value (before run the func)

function itself is stored in the text area of ram
for variable_changable functions, the compiler arrange the parameters normally

=> 9. stream use transfer
just use jar  / auipc + jalr (jump longer)
*** how to deal with insect calling functions parameter distribution problem, how does the inner called function use register resources without 
 disrupt utter functions paramter? 
*** the stored reg s0-s11 & ra (return addr) can not change while inner call process, 
t0-t6(temparary register) & a0-a7(argument register) & ra(return addr) can change 

the "can not change" registers actually could be changed by inner function while processing, but when return back to external function, 
the value must be reversed

Stack Frame
call func: 1. new stack frame, store "unchange" addr to new stack frame, low "my" paramter to register and store "my" others to new stack frame as well
           2. if call inner func, store ra into "my" stack frame, put some parameter which in temparary reg into storage reg
           3. restore stored para & ra, complete processing. add sp for freeing stack frame, return
           

------------------------------------
->
the cost of Instr-grasp miss is much larger than Data-grasp miss
so sometimes compiler tend to use less Instr

->
register: ff
normal ram: latch
modern SRAM is DDR granule: one transistor + one capatitor (capatitor charging has delay, so memory access is slow)
 
-> 
use santisize, compiler could insert "assert()" in the program


