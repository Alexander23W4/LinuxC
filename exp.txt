Macro(Check Log, Conditional Compilation, Representation)
Function Pointer
Accessible Region
Label & goto()



->^PO MACRO:
1.Conditional Compilation  Cross Platform
2.Representation 
3.Debug


-> FUNCTION MACRO:  
#define IOE ({ ioe_init();  })
#define CTE(h) ({ Context *h(Event, Context *); cte_init(h); })
#define VME(f1, f2) ({ void *f1(int); void f2(void *); vme_init(f1, f2); })
#define MPE ({ mpe_init(entry); })

-> PERIOD MACRO: represent a certain block of codes, the example is the representation of "case" block
#define CASE(id, entry_, ...) \
  case id: { \
    void entry_(); \
    entry = entry_; \
    __VA_ARGS__; \
    entry_(); \
    break; \
  }
